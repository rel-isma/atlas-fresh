fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
openpyxl>=3.1
pytest>=8.0
httpx>=0.27

# Optional — only needed if you set ANTHROPIC_API_KEY to enable the
# real LLM assistant provider (app/core/assistant/provider.py:
# LLMProvider). The app runs fully without it; get_default_provider()
# falls back to the deterministic FallbackProvider automatically.
anthropic>=0.40